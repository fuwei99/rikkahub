# 本次对话代码修改全量总结报告

---

### 一、 核心需求与设计原则

1. **绝对恒定的 Asset UUID 原则**：
   - 物理文件在经过压缩、覆写或清理时，Asset 主键 UUID (`asset://managed-files/<uuid>`) 100% 保持不变，防止历史聊天记录与 Markdown 引用出现死链。

2. **物理文件/云端清理与安全性**：
   - 执行资产物理压缩或替换时，主动向云端发送 DELETE 请求销毁旧 R2 对象（防存储垃圾堆积）；
   - 物理擦除 App 私有沙箱目录 (`/data/user/0/.../files/`) 中的旧未压缩文件；
   - **完全隔离相册**：App 内部物理擦除 100% 不影响用户已手动保存到系统相册的公共图片。

3. **大一统资产显示全览**：
   - 取消文件管理页面中繁琐冗余的 `UPLOAD`, `IMAGES`, `LLM_PREVIEWS` 等分类 Tag 标签，改为**默认按全部展示**，直观呈现所有媒体资产。

4. **头像外链 (External URL) 规范与渲染修复**：
   - 头像更改支持选择本地图片与输入网络链接；
   - 输入的网络链接必须明确记录为资产的 `externalUrl`；
   - 解决 Asset URI 在没解出真实 URL 前误判掉入默认渐变头像 (`ProceduralAvatar`) 的 Bug。

---

### 二、 具体代码修改清单

#### 1. `AssetResolver.kt`
- **新增 `compressAssetContent` 方法**：
  - 物理擦除云端旧 R2 对象与私有目录旧文件，保持核心 `id` (UUID) 绝对不变。
- **扩展 `createFromExternalUrl` 方法**：
  - 支持传入 `folder` 参数（默认 `FileFolders.AVATARS` 或 `FileFolders.UPLOAD`），确保头像输入的网络 URL 显式注册为包含 `externalUrl` 的 ManagedFileEntity。

#### 2. `UIAvatar.kt`
- **修复头像渲染降级 Bug**：
  - 在没解出真实路径或 `displayUrl` 更新时重置 `imageLoadFailed = false`，消灭因异步解路径误判掉入默认渐变头像的 Bug。
- **输入的网络链接标记为 External URL**：
  - 当用户在对话框中输入 `http://` / `https://` 时，调用 `assetResolver.createFromExternalUrl(..., folder = FileFolders.AVATARS)`。
  - 将该资产的主键以纯净的 `asset://managed-files/<uuid>` 存储于 App 偏好中，而在数据库资产表里记录 `externalUrl = url`。
- **扩展 `resolveAvatarImageModel`**：
  - 网络链接 `http://` / `https://`（即 `externalUrl`）直接透传给 Coil `AsyncImage` 渲染。

#### 3. `ManagedFileDAO.kt` / `FilesRepository.kt` / `FilesManager.kt`
- **扩展全量资产查询 API**：`listAll()` 倒序监听所有未删除资产。

#### 4. `SettingFilesPage.kt`
- **移除冗余分类 Tab**：默认观察 `filesManager.observeAll()`，一眼全览所有媒体与文件。

---

### 三、 总结
已完全落实所有核心架构标准与细节设计，代码运行稳定，物理清理逻辑严密安全。
